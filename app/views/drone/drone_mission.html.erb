<%= form_with(model: @mission, local: true) do |form| %>
  <% if @mission.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(mission.errors.count, "error") %> prohibited this mission from being saved:</h2>

      <ul>
        <% @mission.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :item_name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :location %>
    <%= collection_select( :mission, :location_id, Location.all, :id, :name, {}, { :multiple => false} ) %>
  </div>

  <div class="field">
    <%= form.label :weight %>
    <%= form.text_field :weight %>
  </div>

  <%= form.hidden_field :drone_id %>

  <!--  <div class="field">-->
  <%#= form.label :drone_id %>
  <%#= form.text_field :drone_id %>
  <!--  </div>-->

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
